<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Globalhitss - Autenticação</title>
    <meta name="description" content="Portal de autenticação corporativa segura da Globalhitss">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', system-ui, -apple-system, sans-serif;
        }

        .glass {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .input-field {
            background: rgba(17, 24, 39, 0.5);
            border: 1px solid #374151;
            transition: all 0.3s ease;
        }

        .input-field:focus {
            border-color: #dc2626;
            box-shadow: 0 0 0 2px rgba(220, 38, 38, 0.2);
        }

        .btn-primary {
            background: #dc2626;
            box-shadow: 0 4px 14px 0 rgba(220, 38, 38, 0.4);
            transition: all 0.3s ease;
        }

        .btn-primary:hover {
            background: #b91c1c;
            transform: translateY(-1px);
            box-shadow: 0 6px 20px 0 rgba(220, 38, 38, 0.5);
        }

        .btn-primary:active {
            transform: translateY(0);
        }

        @keyframes float {
            0% {
                transform: translateY(0px) rotate(0deg);
            }

            50% {
                transform: translateY(-20px) rotate(5deg);
            }

            100% {
                transform: translateY(0px) rotate(0deg);
            }
        }

        @keyframes pulse-glow {

            0%,
            100% {
                opacity: 0.08;
                transform: scale(1);
            }

            50% {
                opacity: 0.15;
                transform: scale(1.05);
            }
        }

        .animate-float {
            animation: float 8s ease-in-out infinite;
        }

        .animate-pulse-glow {
            animation: pulse-glow 6s ease-in-out infinite;
        }

        .logo-glow {
            text-shadow: 0 0 30px rgba(220, 38, 38, 0.3);
        }
    </style>
</head>

<body class="bg-slate-900 text-white min-h-screen flex items-center justify-center overflow-hidden relative">

    <!-- Background Orbs -->
    <div class="absolute inset-0 overflow-hidden pointer-events-none">
        <!-- Red Orb Top Right -->
        <div
            class="absolute -top-32 -right-32 w-[500px] h-[500px] bg-red-600 rounded-full mix-blend-multiply filter blur-[120px] opacity-10 animate-pulse-glow">
        </div>
        <!-- Blue Orb Bottom Left -->
        <div class="absolute -bottom-32 -left-32 w-[500px] h-[500px] bg-blue-800 rounded-full mix-blend-multiply filter blur-[120px] opacity-10 animate-pulse-glow"
            style="animation-delay: 3s;"></div>
        <!-- Small Red Orb -->
        <div
            class="absolute top-1/4 left-1/4 w-[200px] h-[200px] bg-red-500 rounded-full mix-blend-multiply filter blur-[80px] opacity-5 animate-float">
        </div>
    </div>

    <!-- Login Card -->
    <div class="glass rounded-2xl shadow-2xl w-full max-w-md mx-4 p-8 md:p-10 relative z-10">

        <!-- Logo -->
        <div class="text-center mb-8">
            <h1 class="text-4xl font-bold tracking-tighter logo-glow mb-2">
                <span class="text-white">Global</span><span class="text-red-600">hitss</span>
            </h1>
            <p class="text-gray-400 text-sm">Autenticação Corporativa</p>
        </div>

        <!-- Form -->
        <form action="/auth/signin" method="POST" class="space-y-5">
            <!-- Email Field -->
            <div>
                <label for="username" class="block text-xs font-medium text-gray-400 mb-2 uppercase tracking-wider">
                    E-mail Corporativo
                </label>
                <input type="email" name="username" id="username" required autocomplete="email"
                    class="input-field w-full rounded-lg px-4 py-3 text-white text-sm outline-none placeholder-gray-500"
                    placeholder="nome@globalhitss.com.br">
            </div>

            <!-- Password Field -->
            <div>
                <label for="password" class="block text-xs font-medium text-gray-400 mb-2 uppercase tracking-wider">
                    Senha
                </label>
                <input type="password" name="password" id="password" required autocomplete="current-password"
                    class="input-field w-full rounded-lg px-4 py-3 text-white text-sm outline-none placeholder-gray-500"
                    placeholder="••••••••••••">
            </div>

            <!-- Submit Button -->
            <button type="submit"
                class="btn-primary w-full py-3.5 px-4 rounded-lg text-white font-semibold text-sm uppercase tracking-wide mt-2">
                Entrar
            </button>
        </form>

        <!-- Divider -->
        <div class="flex items-center my-6">
            <div class="flex-1 border-t border-gray-700"></div>
            <span class="px-4 text-xs text-gray-500">SEGURANÇA</span>
            <div class="flex-1 border-t border-gray-700"></div>
        </div>

        <!-- Security Notice -->
        <div class="text-center space-y-2">
            <p class="text-[11px] text-gray-500 leading-relaxed">
                Ao clicar em <strong class="text-gray-400">Entrar</strong>, você será redirecionado para validação
                via <span class="text-red-400">Microsoft Authenticator</span> (MFA).
            </p>
            <p class="text-[10px] text-gray-600">
                Protegido por Azure Active Directory
            </p>
        </div>
    </div>

    <!-- Footer -->
    <footer class="absolute bottom-4 left-0 right-0 text-center">
        <p class="text-xs text-gray-600">
            &copy; 2026 Globalhitss. Divisão de Tecnologia Imperial.
        </p>
    </footer>

</body>

</html>